<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: linear-gradient(135deg, #6c757d, #495057);
        color: white;
        font-family: 'Arial', sans-serif;
      }
      .profile-container {
        margin-top: 50px;
        text-align: center;
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
      }
      .profile-container h1 {
        font-size: 2.5rem;
        font-weight: bold;
        margin-bottom: 30px;
        color: #f8f9fa;
      }
      .profile-details {
        background-color: #343a40;
        padding: 40px;
        border-radius: 15px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease-in-out;
      }
      .profile-details:hover {
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        transform: translateY(-5px);
      }
      .profile-details p {
        font-size: 1.2rem;
        margin-bottom: 15px;
      }
      .btn-edit {
        background-color: #007bff;
        color: white;
        padding: 12px 25px;
        border-radius: 50px;
        text-decoration: none;
        font-size: 1rem;
        margin-top: 20px;
        display: inline-block;
        transition: all 0.3s ease-in-out;
      }
      .btn-edit:hover {
        background-color: #0056b3;
        transform: scale(1.05);
      }
      .profile-image {
        border-radius: 50%;
        width: 150px;
        height: 150px;
        margin-bottom: 30px;
        border: 5px solid #007bff;
        object-fit: cover;
      }
      .profile-header {
        display: flex;
        justify-content: center;
        flex-direction: column;
        align-items: center;
      }
      .profile-header h3 {
        font-size: 2rem;
        color: #f8f9fa;
        font-weight: 500;
      }
      .profile-header p {
        font-size: 1.1rem;
        color: #adb5bd;
      }
    </style>
  </head>
  <body>
    <div class="profile-container">
      <div class="profile-header">
        <!-- Profile Picture -->
        <!-- <img src="<%= user.profilePicture || '/images/default-avatar.png' %>" alt="Profile Picture" class="profile-image"> -->
        <h3><%= user.name || 'Anonymous' %></h3>
        <p><%= user.email || 'Anonymous' %></p>
      </div>
      <div class="profile-details">
        <p><strong>Member Since:</strong> <%= user.createdAt.toLocaleDateString() || 'Unknown' %></p>
        <p><strong>Role:</strong> <%= user.role || 'User' %></p>
        
        <!-- Edit Profile Button -->
        <a href="/user/dashboard" class="btn-edit">Go to Home</a>
      </div>
    </div>
  </body>
</html>
